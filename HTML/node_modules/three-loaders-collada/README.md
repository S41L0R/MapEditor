### ThreeJS Collada Loader browserify module

#### Install

```
npm install three-loaders-collada --save-dev
```

#### Usage 
```
var collada = require('three-loaders-collada')(THREE);
this.loader = new THREE.ColladaLoader();
    //// 
};
```